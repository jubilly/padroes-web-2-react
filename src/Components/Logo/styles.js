import styled from 'styled-components';

const LogoContainer = styled.div`
    display: flex;
    font-size: 1.8rem;
`;

const ImageLogo = styled.div`
    margin-right: 10px;
`;

export { LogoContainer, ImageLogo };
