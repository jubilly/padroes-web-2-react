import styled from 'styled-components';

const IconsContainer = styled.div`
    display: flex;
    gap: 24px;
`;

const IconImage = styled.img`
    width: auto;
`;

export { IconsContainer, IconImage };
